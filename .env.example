# Signal Grove environment
# The app runs fully in mock mode with no configuration.
# To switch to a deployed GenLayer contract, set both values below.

# "mock" (default) or "contract"
NEXT_PUBLIC_GROVE_MODE=mock

# Deployed SignalGroveContract address (only used when mode is "contract")
NEXT_PUBLIC_GROVE_CONTRACT=
